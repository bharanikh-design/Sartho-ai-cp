node_modules/
.next/
out/
build/
.env
.env.local
.env.*.local
.DS_Store
*.log
next-env.d.ts
.vercel/
coverage/

# Private account seeds contain personal career evidence and must never be committed.
supabase/seed.sql
supabase/seed.private.sql
supabase/*private*seed*.sql

# The packaged browser extension — rebuilt by `npm run extension:zip`.
# Anchored to the repository root: the served copy at public/sartho-extension.zip
# Built into public/ by the same script on every build, never committed.
public/sartho-extension.zip
# is built by the same script and must reach the deployment, and an unanchored
# pattern here silently swallowed it.
/sartho-extension.zip
# Built into public/ by the same script on every build, never committed.
public/sartho-extension.zip

# TypeScript incremental build cache — regenerated on every build.
tsconfig.tsbuildinfo
.gemini-probe/
